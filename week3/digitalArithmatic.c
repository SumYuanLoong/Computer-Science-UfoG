#include <stdio.h>

void main(){
    int number,i;
    int array[16];

    printf("Hai pls enter z in (x+y)^z: ");
    scanf("%d",&number);

    while (number > 0)
    {
        array[i] = 
        /* code */
    }
    
}